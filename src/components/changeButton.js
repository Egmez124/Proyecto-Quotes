const ChangeButton = ({Change}) => {
    return(
        <div class="d-grid gap-2 col-6 mx-auto">
            <button class="btn btn-primary" type="button" onClick={Change}>Ghange Quote</button>
        </div>
    )
}
export default ChangeButton

